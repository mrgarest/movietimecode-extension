export enum StatusIndicator {
  active = "active",
  inactive = "inactive",
  loader = "loader",
}

export enum ESearchButtonAction {
  searchTimecode = "searchTimecode",
  turnOffCensorship = "turnOffCensorship",
}
