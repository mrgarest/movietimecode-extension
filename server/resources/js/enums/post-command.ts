export enum PostMessageSource {
    SERVER = "movietimecode:server",
    EXTENSION = "movietimecode:extension"
}

export enum PostCommand {
    PING = "PING",
    PONG = "PONG",
    OPEN_TIMECODE_EDITOR = "OPEN_TIMECODE_EDITOR"
}